---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-weave-gitops-ingress
  namespace: flux-system
spec:
  ingress:
    - from:
        - namespaceSelector: {}
  podSelector:
    matchLabels:
      app.kubernetes.io/name: weave-gitops